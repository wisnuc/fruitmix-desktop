const defaultShare = {
	sharePath:[],
	shareChildren: [],
	filesSharedByMe: []
}

const directory = (state = defaultShare,action)=> {
	switch (action.type) {

		default:
			return state
	}
}

module.exports =  directory;