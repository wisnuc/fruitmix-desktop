const mdns2 = (state = [], action) => {
  
  let prev, curr, 

  switch (action.type) {
    
    case 'MDNS_UPDATE':

    case 'MDNS_RESTART':


  default:
    return state
  }
  
}

export default mdns2
